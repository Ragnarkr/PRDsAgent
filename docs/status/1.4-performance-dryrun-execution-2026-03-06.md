# 1.4 性能基线 dry-run 执行记录（2026-03-06）

## 执行目标
- 打通 `三轮原始结果(JSONL) -> baseline计算 -> Gate最差值汇总` 的端到端链路。
- 验证 1.4 口径参数可配置：`500请求 + 20并发 + 10分钟模型 + 预热50`。

## 执行命令
```bash
python scripts/run_perf_baseline_rounds.py \
  --output-dir docs/status/perf-test-data \
  --prefix staging_dryrun_20260306
```

## 产物清单
- `docs/status/perf-test-data/staging_dryrun_20260306_round1.jsonl`
- `docs/status/perf-test-data/staging_dryrun_20260306_round2.jsonl`
- `docs/status/perf-test-data/staging_dryrun_20260306_round3.jsonl`
- `docs/status/perf-test-data/staging_dryrun_20260306_gate_report.json`
- `docs/status/perf-test-data/staging_dryrun_20260306_gate_report.md`

## 指标摘要（dry-run）
- p95_ms(max): `3919.823`（阈值 <= 6000）
- success_rate(min): `99.00%`（阈值 >= 99.00%）
- error_5xx_rate(max): `0.60%`（阈值 <= 0.50%）
- timeout_rate(max): `0.40%`（阈值 <= 0.50%）
- gate_passed: `False`

## 结论与下一步
- 当前结果仅用于“脚本链路可执行”验证，不可作为真实 staging 验收结论。
- 下一步需在真实 staging 固定规格下重跑三轮，并补充资源水位记录（CPU/内存/实例数/错误分布）。
